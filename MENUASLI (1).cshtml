@inject Microsoft.AspNetCore.Antiforgery.IAntiforgery Antiforgery
@{
    Layout = "_Layout";
    var tokens = Antiforgery.GetAndStoreTokens(ViewContext.HttpContext);
}
<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8" />
    <title> غذای اصلی</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/StyleSheet3.css" asp-append-version="true" />
    <meta name="csrf-token" content="@tokens.RequestToken" />
    <style>
        /* فقط وقتی صفحه منو است اعمال شود */
        body.menu-page .profile-btn-container button {
            border-radius: 50%; /* گرد کردن دکمه */
            width: 50px;
            height: 50px;
            font-size: 24px;
            background-color: #007bff;
            color: white;
            border: none;
            box-shadow: 0 4px 6px rgba(0,0,0,0.2);
            transition: transform 0.2s;
        }

            body.menu-page .profile-btn-container button:hover {
                transform: scale(1.1);
            }
    </style>
</head>
<body>
    <header>
        <div class="cart-icon">
            🛒 <span id="cart-count">0</span>
        </div>
        <div class="cart-panel">
            <h3>سبد خرید شما</h3>
            <!-- آیتم‌های سبد اینجا می‌آیند -->
            <div class="modal fade" id="cartModal" tabindex="-1" aria-labelledby="cartModalLabel" aria-hidden="true">
                @Html.AntiForgeryToken()

                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="cartModalLabel">🛒 سبد خرید</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="بستن"></button>
                        </div>
                        <div class="modal-body">
                            <ul id="cart-items" class="list-group"></ul>
                            <hr />
                            <div class="text-end">
                                جمع کل: <span id="total-price">۰</span> تومان
                            </div>
                            <div class="text-end mt-3">
                                <button id="finalize-order" type="button" class="btn btn-success">
                                    نهایی‌سازی سفارش ✅
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </header>
    <div id="loading-screen">
        <div class="spinner"></div>
    </div>

    <form method="post" class="food-item" data-id="11">
        @Html.AntiForgeryToken()
        <img src="https://mohsen-ir.com/menu/admin/images/inner/%7BB24747AA-A11E-D7B7-5861-1ECE598416BF%7D.jpg" alt="چلوکباب سلطانی" class="food-img" />
        <div class="food-info">
            <div class="food-name">چلوکباب سلطانی</div>
            <div class="food-price">75000</div>

            <input type="hidden" name="FoodId" value="11" />
            <input type="hidden" name="ProductName" value="چلوکباب سلطانی" />
            <input type="hidden" name="Price" value="75000" />

            <div class="quantity-controls">
                <button type="button" class="decrease">−</button>
                <input type="text" name="Quantity" class="quantity" value="0" readonly style="width: 30px; text-align: center;" />
                <button type="button" class="increase">+</button>
            </div>

            <button type="button" class="add-to-cart btn btn-primary">افزودن به سبد خرید</button>
        </div>
    </form>

    <form method="post" class="food-item" data-id="12">
        @Html.AntiForgeryToken()

        <img src="https://mohsen-ir.com/menu/admin/images/outer/%7B68D21BA5-6F5A-641E-F724-84FDCCFC0093%7D.jpg" alt="چلو کباب وزیری" class="food-img" />
        <div class="food-info">
            <div class="food-name">چلو کباب وزیری</div>
            <div class="food-price">75000</div>

            <input type="hidden" name="FoodId" value="12" />
            <input type="hidden" name="ProductName" value="چلو کباب وزیری" />
            <input type="hidden" name="Price" value="75000" />

            <div class="quantity-controls">
                <button type="button" class="decrease">−</button>
                <input type="text" name="Quantity" class="quantity" value="0" readonly style="width: 30px; text-align: center;" />
                <button type="button" class="increase">+</button>
            </div>

            <button type="button" class="add-to-cart btn btn-primary">افزودن به سبد خرید</button>
        </div>
    </form>
    <form method="post" class="food-item" data-id="13">
        @Html.AntiForgeryToken()

        <img src="https://mohsen-ir.com/menu/admin/images/outer/%7B0143C29B-0D5B-95E7-9D1C-95B0A38BF1F7%7D.jpg" alt="شيشليک با استخوان" class="food-img" />
        <div class="food-info">
            <div class="food-name">شيشليک با استخوان</div>
            <div class="food-price">75000</div>

            <input type="hidden" name="FoodId" value="13" />
            <input type="hidden" name="ProductName" value="شيشليک با استخوان" />
            <input type="hidden" name="Price" value="75000" />

            <div class="quantity-controls">
                <button type="button" class="decrease">−</button>
                <input type="text" name="Quantity" class="quantity" value="0" readonly style="width: 30px; text-align: center;" />
                <button type="button" class="increase">+</button>
            </div>

            <button type="button" class="add-to-cart btn btn-primary">افزودن به سبد خرید</button>
        </div>
    </form>
    <form method="post" class="food-item" data-id="14">
        @Html.AntiForgeryToken()

        <img src="https://mohsen-ir.com/menu/admin/images/inner/%7B94D60E6F-4CAF-BE3D-D254-847186A1D764%7D.jpg" alt="چلوکباب برگ" class="food-img" />
        <div class="food-info">
            <div class="food-name">چلوکباب برگ</div>
            <div class="food-price">75000</div>

            <input type="hidden" name="FoodId" value="14" />
            <input type="hidden" name="ProductName" value="چلوکباب برگ" />
            <input type="hidden" name="Price" value="75000" />

            <div class="quantity-controls">
                <button type="button" class="decrease">−</button>
                <input type="text" name="Quantity" class="quantity" value="0" readonly style="width: 30px; text-align: center;" />
                <button type="button" class="increase">+</button>
            </div>

            <button type="button" class="add-to-cart btn btn-primary">افزودن به سبد خرید</button>
        </div>
    </form>
    <form method="post" class="food-item" data-id="15">
        @Html.AntiForgeryToken()

        <img src="https://mohsen-ir.com/menu/admin/images/inner/%7B01C91E97-DA37-3BB6-C64C-A0DAAE339217%7D.jpg" alt="جوجه کباب با برنج" class="food-img" />
        <div class="food-info">
            <div class="food-name">جوجه کباب با برنج</div>
            <div class="food-price">75000</div>

            <input type="hidden" name="FoodId" value="15" />
            <input type="hidden" name="ProductName" value="جوجه کباب با برنج" />
            <input type="hidden" name="Price" value="75000" />

            <div class="quantity-controls">
                <button type="button" class="decrease">−</button>
                <input type="text" name="Quantity" class="quantity" value="0" readonly style="width: 30px; text-align: center;" />
                <button type="button" class="increase">+</button>
            </div>

            <button type="button" class="add-to-cart btn btn-primary">افزودن به سبد خرید</button>
        </div>
    </form>
    <form method="post" class="food-item" data-id="16">
        @Html.AntiForgeryToken()

        <img src="https://mohsen-ir.com/menu/admin/images/outer/%7BA96AE2F0-5131-2F1B-FAD4-0AC2E6B602A8%7D.jpg" alt="زرشک پلو با مرغ" class="food-img" />
        <div class="food-info">
            <div class="food-name">زرشک پلو با مرغ</div>
            <div class="food-price">75000</div>

            <input type="hidden" name="FoodId" value="16" />
            <input type="hidden" name="ProductName" value="زرشک پلو با مرغ" />
            <input type="hidden" name="Price" value="75000" />

            <div class="quantity-controls">
                <button type="button" class="decrease">−</button>
                <input type="text" name="Quantity" class="quantity" value="0" readonly style="width: 30px; text-align: center;" />
                <button type="button" class="increase">+</button>
            </div>

            <button type="button" class="add-to-cart btn btn-primary">افزودن به سبد خرید</button>
        </div>
    </form>
    <form method="post" class="food-item" data-id="17">
        @Html.AntiForgeryToken()

        <img src="https://mohsen-ir.com/menu/admin/images/outer/%7B455EB2B1-80CC-52FF-9D03-25D5CA5E5791%7D.jpg" alt="آلبالو پلو با مرغ" class="food-img" />
        <div class="food-info">
            <div class="food-name">آلبالو پلو با مرغ</div>
            <div class="food-price">75000</div>

            <input type="hidden" name="FoodId" value="17" />
            <input type="hidden" name="ProductName" value="آلبالو پلو با مرغ" />
            <input type="hidden" name="Price" value="75000" />

            <div class="quantity-controls">
                <button type="button" class="decrease">−</button>
                <input type="text" name="Quantity" class="quantity" value="0" readonly style="width: 30px; text-align: center;" />
                <button type="button" class="increase">+</button>
            </div>

            <button type="button" class="add-to-cart btn btn-primary">افزودن به سبد خرید</button>
        </div>
    </form>
    <form method="post" class="food-item" data-id="18">
        @Html.AntiForgeryToken()


        <img src="https://mohsen-ir.com/menu/admin/images/outer/%7B29757FCE-40EE-182E-855A-0204C39D85B9%7D.jpg" alt="چلو خورشت قرمه سبزی" class="food-img" />
        <div class="food-info">
            <div class="food-name">چلو خورشت قرمه سبزی</div>
            <div class="food-price">75000</div>

            <input type="hidden" name="FoodId" value="18" />
            <input type="hidden" name="ProductName" value="چلو خورشت قرمه سبزی" />
            <input type="hidden" name="Price" value="75000" />

            <div class="quantity-controls">
                <button type="button" class="decrease">−</button>
                <input type="text" name="Quantity" class="quantity" value="0" readonly style="width: 30px; text-align: center;" />
                <button type="button" class="increase">+</button>
            </div>

            <button type="button" class="add-to-cart btn btn-primary">افزودن به سبد خرید</button>
        </div>
    </form>
    <form method="post" class="food-item" data-id="19">
        @Html.AntiForgeryToken()

        <img src="https://mohsen-ir.com/menu/admin/images/outer/%7B20B46B70-0177-7704-E3E0-CCC8C957F613%7D.jpg" alt="چلو خورشت قيمه سيب زمينی" class="food-img" />
        <div class="food-info">
            <div class="food-name">چلو خورشت قيمه سيب زمينی</div>
            <div class="food-price">75000</div>

            <input type="hidden" name="FoodId" value="19" />
            <input type="hidden" name="ProductName" value="چلو خورشت قيمه سيب زمينی" />
            <input type="hidden" name="Price" value="75000" />

            <div class="quantity-controls">
                <button type="button" class="decrease">−</button>
                <input type="text" name="Quantity" class="quantity" value="0" readonly style="width: 30px; text-align: center;" />
                <button type="button" class="increase">+</button>
            </div>

            <button type="button" class="add-to-cart btn btn-primary">افزودن به سبد خرید</button>
        </div>
    </form>
    <form method="post" class="food-item" data-id="20">
        @Html.AntiForgeryToken()

        <img src="https://mohsen-ir.com/menu/admin/images/outer/%7B49CEDF67-C519-48B9-0EE1-96D9462B7890%7D.jpg" alt="آلبالو پلو" class="food-img" />
        <div class="food-info">
            <div class="food-name">آلبالو پلو</div>
            <div class="food-price">75000</div>

            <input type="hidden" name="FoodId" value="20" />
            <input type="hidden" name="ProductName" value="آلبالو پلو" />
            <input type="hidden" name="Price" value="75000" />

            <div class="quantity-controls">
                <button type="button" class="decrease">−</button>
                <input type="text" name="Quantity" class="quantity" value="0" readonly style="width: 30px; text-align: center;" />
                <button type="button" class="increase">+</button>
            </div>

            <button type="button" class="add-to-cart btn btn-primary">افزودن به سبد خرید</button>
        </div>
    </form>
    <!-- دکمه‌های ناوبری -->
    <div class="d-flex justify-content-between mt-4 nav-buttons">
        @Html.AntiForgeryToken()
        <!-- دکمه قبلی -->
        <a href="/Home/MENU" class="btn btn-outline-danger">
            قبلی
        </a>
        <!-- دکمه بعدی -->
        <a href="/Home/PASSMENU" class="btn btn-outline-danger">
            بعدی
        </a>
    </div>
    <br />
    <br />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            // ابتدا شمارنده سبد خرید را آپدیت کن
            updateCartCount();

            // افزودن به سبد خرید
            document.querySelectorAll('.add-to-cart').forEach(button => {
                button.addEventListener('click', () => {
                    const form = button.closest('.food-item');
                    const foodId = form.querySelector('input[name="FoodId"]').value;
                    const quantity = form.querySelector('input[name="Quantity"]').value;
                    const token = form.querySelector('input[name="__RequestVerificationToken"]').value;

                    if (parseInt(quantity) <= 0) {
                        alert("لطفاً تعداد را بیشتر از صفر وارد کنید.");
                        return;
                    }

                    const data = new URLSearchParams();
                    data.append('__RequestVerificationToken', token);
                    data.append('FoodId', foodId);
                    data.append('Quantity', quantity);

                    fetch('/Home/AddToCart', {
                        method: 'POST',
                        credentials: 'include',
                        headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
                        body: data
                    })
                        .then(res => res.json())
                        .then(result => {
                            if (result.success) {
                                updateCartCount();
                                loadCartItems();

                                // باز کردن مودال
                                const cartModal = new bootstrap.Modal(document.getElementById('cartModal'));
                                cartModal.show();

                                // ریست تعداد بعد از اضافه شدن
                                form.querySelector('.quantity').value = 0;
                            } else {
                                alert(result.message || "خطا در افزودن به سبد خرید");
                            }
                        });
                });
            });

            // افزایش تعداد
            document.querySelectorAll('.increase').forEach(button => {
                button.addEventListener('click', () => {
                    const quantityInput = button.closest('.quantity-controls').querySelector('.quantity');
                    quantityInput.value = parseInt(quantityInput.value) + 1;
                });
            });

            // کاهش تعداد
            document.querySelectorAll('.decrease').forEach(button => {
                button.addEventListener('click', () => {
                    const quantityInput = button.closest('.quantity-controls').querySelector('.quantity');
                    if (parseInt(quantityInput.value) > 0) {
                        quantityInput.value = parseInt(quantityInput.value) - 1;
                    }
                });
            });
        });

        // بارگذاری اقلام سبد خرید
        function loadCartItems() {
            fetch('/Home/GetCart', { method: 'GET', credentials: 'include' })
                .then(res => res.json())
                .then(data => {
                    const cartList = document.getElementById('cart-items');
                    const totalPriceEl = document.getElementById('total-price');
                    cartList.innerHTML = '';

                    if (!data.items || data.items.length === 0) {
                        cartList.innerHTML = '<li class="list-group-item text-center">سبد خرید خالی است</li>';
                        totalPriceEl.textContent = '۰';
                        return;
                    }

                    let total = 0;
                    data.items.forEach(item => {
                        const li = document.createElement('li');
                        li.className = 'list-group-item d-flex justify-content-between';
                        li.textContent = `${item.ProductName} × ${item.Quantity}`;
                        const priceDiv = document.createElement('div');
                        priceDiv.textContent = item.Total.toLocaleString() + " تومان";
                        li.appendChild(priceDiv);
                        cartList.appendChild(li);
                        total += item.Total;
                    });
                    totalPriceEl.textContent = total.toLocaleString();
                });
        }

        // بروزرسانی شمارنده سبد خرید
        function updateCartCount() {
            fetch('/Home/GetCart', { method: 'GET', credentials: 'include' })
                .then(res => res.json())
                .then(data => {
                    const count = data.items ? data.items.reduce((sum, item) => sum + item.Quantity, 0) : 0;
                    document.getElementById('cart-count').textContent = count;
                });
        }
        // مخفی کردن لودینگ اسکرین
        window.onload = () => {
            setTimeout(() => {
                const loadingScreen = document.getElementById('loading-screen');
                if (loadingScreen) loadingScreen.style.display = 'none';
            }, 1000);
        };
    </script>
    <script>
        window.onload = function () {
            setTimeout(function () {
                const loadingScreen = document.getElementById('loading-screen');
                loadingScreen.style.display = 'none';
            }, 1000);
        };
    </script>
</body>
</html>

